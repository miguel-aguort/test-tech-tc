name: test_env                         # env was made with --prefix
channels:
  - conda-forge                     # added a third party channel
  - defaults
dependencies:
  - python==3.12
  - pip:                            # Packages installed from PyPI
    - docling==2.58.0
    - pandas==2.3.3
    - fastparquet==2024.11.0
    - datasets==4.3.0
    - python-dotenv==1.2.1

    - sqlite-vec==0.1.6
    - langchain-community==0.4.0
    - sentence-transformers==5.1.2

    - langgraph==1.0.1

    - gradio==5.49.1