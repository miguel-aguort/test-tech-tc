name: test_env_langgraph                          # env was made with --prefix
channels:
  - conda-forge                     # added a third party channel
  - defaults
dependencies:
  - python==3.12
  - pip:                            # Packages installed from PyPI
    - langgraph==1.0.1