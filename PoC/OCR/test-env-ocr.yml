name: test_env_ocr                         # env was made with --prefix
channels:
  - conda-forge                     # added a third party channel
  - defaults
dependencies:
  - python==3.12
  - pip:                            # Packages installed from PyPI
    - docling==2.58.0